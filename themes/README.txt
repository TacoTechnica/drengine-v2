
Add editor themes here. For now the engine doesn't read them automatically, but that feature will arrive eventually.

Make sure the folder has a "gtk.css" file in there somewhere, as the editor will be using that to style itself.
